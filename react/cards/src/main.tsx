import React from 'react'
import ReactDOM from 'react-dom/client'
import './styles.css'
import CardsStarter from './CardsStarter'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CardsStarter />
  </React.StrictMode>,
)
