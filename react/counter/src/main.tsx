import React from 'react'
import ReactDOM from 'react-dom/client'
import CounterStarter from "./CounterStarter.tsx";

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <CounterStarter/>
    </React.StrictMode>,
)
