import React from 'react'
import ReactDOM from 'react-dom/client'
import CategoriesSolution from './CategoriesSolution.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CategoriesSolution />
  </React.StrictMode>,
)
