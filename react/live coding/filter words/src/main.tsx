import React from 'react'
import ReactDOM from 'react-dom/client'
import FilterWordsStarter from "./FilterWordsStarter.tsx";

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FilterWordsStarter />
  </React.StrictMode>,
)
