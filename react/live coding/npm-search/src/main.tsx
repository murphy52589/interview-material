import React from 'react'
import ReactDOM from 'react-dom/client'
import NpmSearchStarter from "./starter/search.tsx";

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <NpmSearchStarter />
  </React.StrictMode>,
)
