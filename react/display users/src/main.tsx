import React from 'react'
import ReactDOM from 'react-dom/client'
import DisplayUsersStarter from './DisplayUsersStarter.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DisplayUsersStarter />
  </React.StrictMode>,
)
